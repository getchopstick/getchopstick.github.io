Repository for the getchopstick.com website
-------------------------------------------

* First run: `gem update --system && gem install bundler && bundle install`
* Then always: `bundle exec guard`
